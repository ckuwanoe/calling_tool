<%= check_box_tag 'survey_reply[responses_attributes][' + index.to_s + '][answer_id]', 
answers.id, @survey_reply.responses.map(&:answer_id).include?(answers.id) %>
<%= f.label :answer_id, answers.content %><br/>
